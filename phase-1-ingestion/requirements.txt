pydantic>=2,<3
PyYAML>=6
pytest>=8
# Allows PEP 604 (`X | None`) annotations to resolve on Python 3.9; no-op on 3.10+.
eval_type_backport>=0.2; python_version < "3.10"
# Language filtering (deterministic with a fixed seed).
langdetect>=1.0.9
# Live Google Play fetching (optional at runtime; tests use fixtures, not the network).
google-play-scraper>=1.2.6
