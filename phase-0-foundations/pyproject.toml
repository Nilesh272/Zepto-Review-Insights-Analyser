[project]
name = "pulse-phase0"
version = "0.0.0"
description = "Weekly Product Review Pulse — Phase 0 (Foundations & Scaffolding)"
requires-python = ">=3.11"
dependencies = ["pydantic>=2,<3", "PyYAML>=6"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
